---
language: python
services: docker

install:
  # Install test dependencies.
  - pip install molecule yamllint ansible-lint testinfra docker

script:
  # Run tests.
  - molecule --version
  - ansible --version
  - molecule test
